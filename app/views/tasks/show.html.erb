<h1><%= t(".title") %></h1>

<div>
  <%= t("activerecord.attributes.task.title") %>
  <%= @task.title %>
</div>

<div>
  <%= t("activerecord.attributes.task.content") %>
  <%= @task.content %>
</div>

<div>
  <%= t("activerecord.attributes.task.deadline_on") %>
  <%= @task.deadline_on %>
</div>

<div>
  <%= t("activerecord.attributes.task.priority") %>
  <%= I18n.t("enums.task.priority_enum.#{@task[:priority]}") %>
</div>

<div>
  <%= t("activerecord.attributes.task.status") %>
  <%= I18n.t("enums.task.status_enum.#{@task[:status]}") %>
</div>

<%= link_to t("common.edit"), edit_task_path(@task), id: "edit-task" %> |
<%= link_to t("common.back"), tasks_path, id: "back" %>